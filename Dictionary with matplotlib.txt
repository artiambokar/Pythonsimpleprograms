Create a dictionary to store the frequency of each element in L2. Iterate through L2 once, incrementing the count for each element in the dictionary.

import matplotlib.pyplot as plt

hours=[2,4,6,8,10]
marks=[50,60,70,80,100]
plt.plot(hours, marks)
plt.show()
